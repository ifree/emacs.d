;;; env-config.el --- default config

;;; Commentary:
;; 

;;; Code:

(use-package theme-config :demand)
(use-package window-config :demand)
(use-package editor-config :demand)




(provide 'env-config)

;;; env-config.el ends here
